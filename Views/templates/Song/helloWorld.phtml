<?php
// Include necessary files
include __DIR__."/../../../Domains/Song.php";
include __DIR__."/../../../Models/Song.php";


// Database connection setup
try {
    // Instantiate the Songs model
    $songModel = new Models_Song();

    // Fetch a song by ID (e.g., ID = 1)
    $song = $songModel->findById(1);

} catch (PDOException $e) {
    echo "Connection failed: " . $e->getMessage();
}
?>

<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hello World</title>
</head>
<body>
<h1>Hallo aus helloWorld</h1>
<?php if (isset($song)): ?>
    <h2>Song Details</h2>
    <p><strong>Song Name:</strong> <?= htmlspecialchars($song->name) ?></p>
    <p><strong>Artist:</strong> <?= htmlspecialchars($song->artist) ?></p>
    <p><strong>Genre:</strong> <?= htmlspecialchars($song->genre) ?></p>
    <p><strong>Mood:</strong> <?= htmlspecialchars($song->mood) ?></p>
    <p><strong>Length:</strong> <?= htmlspecialchars($song->length) ?> seconds</p>
<?php else: ?>
    <p>Kein Song gefunden.</p>
<?php endif; ?>
</body>
</html>